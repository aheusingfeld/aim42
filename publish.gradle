
buildscript {
    repositories {
        mavenLocal()
        mavenCentral()
        jcenter()
    }

    dependencies {
        classpath 'org.ajoberstar:gradle-git-publish:0.3.0'
    }
}


apply plugin: 'org.ajoberstar.git-publish'
gitPublish {
  token = System.getenv('GH_TOKEN')
  System.setProperty('org.ajoberstar.grgit.auth.username', token)
  repoUri = 'https://github.com/aim42/aim42.github.io.git'
  branch = 'master'
  contents {
    from('build/docs/html5')
  }
}

